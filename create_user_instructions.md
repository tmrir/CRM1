-- ========================================
-- إنشاء مستخدم في Supabase Auth
-- ========================================

-- ملاحظة: هذا الأمر يجب تنفيذه من لوحة تحكم Supabase
-- أو عبر API وليس عبر SQL مباشرة

-- بيانات المستخدم الافتراضي:
-- Email: admin@crm.com
-- Password: admin123
-- Role: Admin

-- بعد إنشاء المستخدم في Supabase Auth، سيتم ربطه تلقائياً
-- بسجل الموظف الذي أنشأناه في جدول employees

-- ========================================
-- خطوات إنشاء المستخدم في لوحة تحكم Supabase:
-- ========================================

-- 1. اذهب إلى لوحة تحكم Supabase
-- 2. اختر Authentication > Users
-- 3. اضغط على "Add user"
-- 4. أدخل البيانات التالية:
--    - Email: admin@crm.com
--    - Password: admin123
-- 5. اضغط على "Save"

-- ========================================
-- بعد إنشاء المستخدم، قم بتحديث جدول employees
-- ========================================

-- احصل على UUID للمستخدم من Supabase Auth
-- ثم قم بتحديث سجل الموظف:

-- UPDATE employees 
-- SET id = 'USER_UUID_FROM_SUPABASE_AUTH' 
-- WHERE username = 'admin';
