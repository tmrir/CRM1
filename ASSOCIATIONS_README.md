# نظام إدارة الجمعيات - دليل الاستخدام

## نظرة عامة
تم إضافة قسم جديد لإدارة الجمعيات في نظام CRM مع ميزات متقدمة لتتبع التواصل والاستجابة.

## الميزات الرئيسية

### 1. **تصنيف الجمعيات حسب الحالة**
- **جديد**: جمعيات تم إضافتها حديثاً
- **تم التواصل**: جمعيات تم التواصل معها
- **لم يتم التواصل**: جمعيات لم يتم التواصل معها بعد
- **معدل الاستجابة**: جمعيات مع تتبع نسبة الاستجابة

### 2. **الإضافة السريعة والاستيراد** ✨ (الميزة الجديدة الرئيسية)

#### اختيار مكان التخزين قبل الإضافة:
عند إضافة بيانات جديدة (نسخ/لصق أو استيراد)، يمكنك اختيار القسم الذي ستُضاف إليه البيانات:
- ✅ في قسم تم التواصل
- ✅ في قسم لم يتم التواصل  
- ✅ في قسم معدل الاستجابة
- ✅ جديد (الكل)

#### طريقة الاستخدام:
1. اضغط على زر "+ إضافة سريعة / استيراد"
2. اختر القسم المستهدف من الخيارات أعلاه
3. الصق البيانات بالتنسيق: `اسم الجمعية | المدينة | الفئة | البريد | الهاتف`
4. أو اختر ملف للاستيراد (PDF/Excel)

### 3. **نقل وترحيل البيانات** 🔄

#### نقل البيانات المختارة:
- حدد جمعية واحدة أو أكثر باستخدام صناديق الاختيار
- استخدم أزرار النقل السريع:
  - **نقل إلى تم التواصل**
  - **نقل إلى لم يتم التواصل**
  - **نقل إلى معدل الاستجابة** (مع إمكانية تحديد النسبة)

#### تحديد نسبة الاستجابة:
عند النقل إلى قسم "معدل الاستجابة"، ستظهر نافذة لتحديد النسبة (0-100%)

### 4. **نسخ الأعمدة** 📋

أزرار سريعة لنسخ جميع البيانات من عمود معين:
- 📱 **نسخ جميع أرقام الجوال**
- 📧 **نسخ جميع البريد الإلكتروني**
- 🌐 **نسخ جميع المواقع الإلكترونية**

### 5. **البحث والتصفية**
- بحث بالاسم أو المدينة
- تصفية حسب الحالة (اضغط على البطاقات الإحصائية)
- عرض العدد الإجمالي لكل فئة

## إعداد قاعدة البيانات

قم بتشغيل ملف `associations_table.sql` في قاعدة بيانات Supabase:

\`\`\`sql
-- سيقوم بإنشاء جدول associations مع جميع الحقول المطلوبة
-- والفهارس لتحسين الأداء
\`\`\`

## الوصول إلى القسم

1. سجل الدخول إلى النظام
2. من القائمة الجانبية، اضغط على **"الجمعيات"** 🏢
3. ستظهر لك لوحة التحكم الكاملة

## أمثلة على الاستخدام

### مثال 1: إضافة جمعيات تم التواصل معها
1. افتح قسم الإضافة السريعة
2. اختر "في قسم تم التواصل"
3. الصق البيانات:
\`\`\`
جمعية البركة | الرياض | خيرية | info@baraka.org | 0501234567
جمعية الأمل | جدة | أيتام | hope@alamal.org | 0509876543
\`\`\`
4. اضغط "إضافة البيانات"

### مثال 2: نقل جمعيات إلى معدل الاستجابة
1. حدد الجمعيات المطلوبة
2. اضغط "نقل إلى معدل الاستجابة"
3. حدد النسبة (مثلاً 75%)
4. اضغط "تأكيد ونقل"

## الحل الجذري للمشكلة ✅

**المشكلة الأصلية**: كانت البيانات المضافة تذهب دائماً إلى قائمة الجمعيات العامة بغض النظر عن القسم المطلوب.

**الحل المطبق**:
1. إضافة خيارات اختيار القسم المستهدف قبل الإضافة
2. استخدام حالة `targetStatus` لتحديد الوجهة
3. تطبيق الحالة المختارة على جميع البيانات المضافة
4. إضافة إمكانية الترحيل بين الأقسام بعد الإضافة

## الملفات المعدلة

- `types.ts`: إضافة نوع Association
- `components/AssociationsView.tsx`: المكون الرئيسي الجديد
- `components/Sidebar.tsx`: إضافة رابط الجمعيات
- `App.tsx`: دمج المكون في التطبيق
- `associations_table.sql`: سكريبت قاعدة البيانات

## ملاحظات مهمة

- جميع البيانات تُخزن محلياً في الوقت الحالي (useState)
- للربط مع Supabase، ستحتاج إلى إضافة دوال API
- تأكد من تشغيل سكريبت SQL قبل الاستخدام الفعلي
- مكتبة XLSX محملة عبر CDN في index.html

## الدعم الفني

للمساعدة أو الإبلاغ عن مشاكل، يرجى التواصل مع فريق التطوير.
